import React from "react";

function Logo({
    className="",
}) {
    return <div>
        <img src="images/vaiakshLogo.png" className={`${className} `} alt="" />
    </div>;
}

export default Logo;
